# whisper.cpp Runner

This folder is reserved for native Whisper transcription through whisper.cpp.

The first implementation should target `/v1/audio/transcriptions` and the `SpeechToTextEngine` trait without coupling the API route to whisper.cpp internals.

