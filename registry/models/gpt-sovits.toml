id = "gpt-sovits"
name = "GPT-SoVITS"
family = "gpt-sovits"
version = "0.1.0"
kind = "voice-cloning"
backend = "python-managed"
runner = "takokit-python-managed"
license = "research-check-required"
description = "Few-shot voice cloning and TTS package placeholder. Metadata-only; GPL/runtime dependency risks must be reviewed before execution support."

[capabilities]
tts = true
stt = false
voice_cloning = true
live_transcription = false
live_audio = true

[hardware]
cpu = true
gpu = true
min_ram = "8gb"

[artifacts]
metadata_only = true
weights = []
voices = []
