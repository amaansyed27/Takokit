id = "xtts-v2"
name = "XTTS v2"
family = "xtts-v2"
version = "2.0.0"
kind = "voice-cloning"
backend = "python-managed"
runner = "takokit-python-managed"
required_adapter = "coqui_tts"
license = "coqui-public-model-license-check-required"
description = "Coqui XTTS v2 multilingual zero-shot voice cloning through the official TTS Python API. Requires a consent-backed voice profile or reference audio."

[capabilities]
tts = true
stt = false
voice_cloning = true
live_transcription = false
live_audio = true

[hardware]
cpu = true
gpu = true
min_ram = "12gb"

[artifacts]
metadata_only = false
weights = []
voices = []
