id = "voxtral"
name = "Voxtral"
family = "voxtral"
version = "0.1.0"
kind = "omni-audio"
backend = "python-managed"
runner = "takokit-python-managed"
required_adapter = "voxtral"
license = "license-check-required"
description = "Voxtral Mini multilingual speech transcription and audio-language inference through the official Transformers API in an isolated managed environment."

[capabilities]
tts = false
stt = true
voice_cloning = false
live_transcription = false
live_audio = true

[hardware]
cpu = true
gpu = true
min_ram = "16gb"

[artifacts]
metadata_only = false
weights = []
voices = []
