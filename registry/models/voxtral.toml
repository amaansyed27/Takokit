id = "voxtral"
name = "Voxtral"
version = "0.1.0"
kind = "omni-audio"
backend = "transformers-audio"
runner = "takokit-transformers-audio"
license = "license-check-required"
description = "Voxtral audio-language model family. Metadata-only; no Transformers audio adapter or verified artifacts are installed."

[capabilities]
tts = false
stt = true
voice_cloning = false
live_transcription = true
live_audio = true

[hardware]
cpu = true
gpu = true
min_ram = "16gb"

[artifacts]
metadata_only = true
weights = []
voices = []
