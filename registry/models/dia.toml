id = "dia"
name = "Dia"
version = "0.1.0"
kind = "tts"
backend = "python-managed"
runner = "takokit-python-managed"
license = "license-check-required"
description = "Dia dialogue and speech generation family. Metadata-only; no verified artifacts or managed Python adapter are installed."

[capabilities]
tts = true
stt = false
voice_cloning = false
live_transcription = false
live_audio = true

[hardware]
cpu = true
gpu = true
min_ram = "12gb"

[artifacts]
metadata_only = true
weights = []
voices = []
