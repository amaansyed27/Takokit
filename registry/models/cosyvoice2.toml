id = "cosyvoice2"
name = "CosyVoice2"
version = "0.1.0"
kind = "voice-cloning"
backend = "python-managed"
runner = "takokit-python-managed"
license = "license-check-required"
description = "CosyVoice2 TTS and voice cloning family. Metadata-only; no verified artifacts or managed runtime adapter are installed."

[capabilities]
tts = true
stt = false
voice_cloning = true
live_transcription = false
live_audio = true

[hardware]
cpu = true
gpu = true
min_ram = "16gb"

[artifacts]
metadata_only = true
weights = []
voices = []
