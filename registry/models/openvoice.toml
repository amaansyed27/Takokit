id = "openvoice"
name = "OpenVoice"
family = "openvoice"
version = "0.1.0"
kind = "voice-cloning"
backend = "python-managed"
runner = "takokit-python-managed"
license = "license-check-required"
description = "OpenVoice voice cloning and conversion family. Metadata-only; runtime dependency and license risk must be reviewed before execution support."

[capabilities]
tts = true
stt = false
voice_cloning = true
live_transcription = false
live_audio = true

[hardware]
cpu = true
gpu = true
min_ram = "8gb"

[artifacts]
metadata_only = true
weights = []
voices = []
