id = "sensevoice"
name = "SenseVoice"
version = "0.1.0"
kind = "stt"
backend = "python-managed"
runner = "takokit-python-managed"
license = "license-check-required"
description = "SenseVoice speech recognition family. Metadata-only; no verified artifacts or managed runtime adapter are installed."

[capabilities]
tts = false
stt = true
voice_cloning = false
live_transcription = true
live_audio = false

[hardware]
cpu = true
gpu = true
min_ram = "8gb"

[artifacts]
metadata_only = true
weights = []
voices = []
