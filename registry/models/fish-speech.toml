id = "fish-speech"
name = "Fish Speech"
family = "fish-speech"
version = "0.1.0"
kind = "voice-cloning"
backend = "python-managed"
runner = "takokit-python-managed"
license = "license-check-required"
description = "Fish Speech TTS and voice cloning family. Metadata-only; Takokit has no verified weights or managed runtime adapter yet."

[capabilities]
tts = true
stt = false
voice_cloning = true
live_transcription = false
live_audio = true

[hardware]
cpu = true
gpu = true
min_ram = "16gb"

[artifacts]
metadata_only = true
weights = []
voices = []
