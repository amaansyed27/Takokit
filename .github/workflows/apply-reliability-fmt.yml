name: Apply reliability formatting

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-reliability-fmt.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all
      - name: Commit formatting
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No formatting changes required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/cli/src/daemon_client.rs apps/cli/src/daemon_commands.rs crates/takokit-package/src/artifact_io.rs crates/takokit-package/src/error.rs crates/takokit-server/src/handlers/error.rs
          git commit -m "Format reliability changes"
          git push origin HEAD:main
